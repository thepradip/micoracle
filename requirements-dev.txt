# Development + test deps. Install with:
#     pip install -r requirements-dev.txt
#
# These are NOT needed to run the agent, only to run the test suite.

-r requirements.txt

pytest>=8.0
pytest-cov>=4.0
